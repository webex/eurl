FROM node:8.16.0-alpine

WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .

ENV PORT=
ENV BASE_URL=
ENV MONGO_URI=
ENV CISCOSPARK_ACCESS_TOKEN=
ENV WEBEXTEAMS_ADMIN_SPACE_ID=
ENV WEBEXTEAMS_SUPPORT_SPACE_ID=
ENV WEBEXTEAMS_WEBHOOK_SECRET=
ENV SUPPORT_EMAIL=

CMD [ "npm", "start" ]
